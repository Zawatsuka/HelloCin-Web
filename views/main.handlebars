<div class="container-fluid onbackgroundLT">
    <h2 class="centerText">Les plus gros succés <span class="colorsOfTitle">Films</span></h2>
    <div class="row margOnFilmLT">
        {{#each results.filmAccueil}}
        <div class="col-3 mt-5">
            <a href="/movieDetail/{{this.id}}">
                <img src="https://image.tmdb.org/t/p/original/{{this.poster_path}}" width="200"
                    class="img-fluid viewDescriptionLT" alt="{{this.title}}">
            </a>
        </div>
        {{/each}}

    </div>

</div>
<div class="container-fluid onbackgroundLT marginSpaceLT">
    <h2 class="centerTextLT">Les plus gros succés <span class="colorsOfTitleLT">Series</span></h2>
    <div class="row margOnFilmLT">
        {{#each results.seriesAccueil}}
        <div class="col-3 mt-5">
             <a href="/serieDetail/{{this.id}}">
            <img src="https://image.tmdb.org/t/p/original/{{this.poster_path}}" width="200"
                class="img-fluid viewDescription" alt="{{this.title}}">
            </a>
            </div>
        {{/each}}

    </div>

</div>