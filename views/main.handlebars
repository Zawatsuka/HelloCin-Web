<div class="container-fluid backgroundImg">
    <a href="#" onclick="openNav()" class="p-5"><img class="burger" src="./assets/img/burger.png" alt=""></a>
    <img src="./assets/img/logo_2.png" class="img-fluid logo" width="120" alt="">

</div>

<div class="container-fluid onbackground">
    <h2 class="">Les plus gros succés <span class="colorsOfTitle">Films</span></h2>
    <div class="row">
        {{#each results.film}}
        <div class="col-3 margOnFilm ">
            <img src="https://image.tmdb.org/t/p/original/{{this.poster_path}}" width="200" class="img-fluid"
                alt="{{this.title}}">
            <p class="titleOfMovie">{{this.title}}</p>
             <a class="btn btn-primary" data-toggle="collapse" href="#collapseExample" role="button" aria-expanded="false" aria-controls="collapseExample">
                <img src="" alt=""> 
            </a>
        </div>
        {{/each}}
    </div>
</div>